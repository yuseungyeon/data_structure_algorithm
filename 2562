from sys import stdin

max_index = 0
max_num = 0

for i in range(1,10):
    num = int(stdin.readline())
    if num > max_num:
        max_num = num
        max_index = i

print(max_num, max_index, sep = '\n')
