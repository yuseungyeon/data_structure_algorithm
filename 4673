array = [int(i) for i in range(1, 10001)]

for i in range(1, 10001):
    num = str(i)
    ans = i
    for j in num:
        ans += int(j)
    if ans in array:
        array.remove(ans)

print(*array, sep ='\n')
